{
  "name": "libre-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "cargo test",
    "test:watch": "cargo test --watch",
    "clippy": "cargo clippy -- -D warnings",
    "check": "cargo check",
    "clean": "cargo clean",
    "release": "./scripts/release.sh release",
    "release:build": "./scripts/release.sh build",
    "release:test": "./scripts/release.sh test",
    "release:bump": "./scripts/release.sh bump",
    "release:tag": "./scripts/release.sh tag",
    "release:push": "./scripts/release.sh push",
    "version:patch": "./scripts/release.sh bump patch && ./scripts/release.sh tag",
    "version:minor": "./scripts/release.sh bump minor && ./scripts/release.sh tag",
    "version:major": "./scripts/release.sh bump major && ./scripts/release.sh tag"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-http": "^2.5.2",
    "@tauri-apps/plugin-opener": "^2",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10",
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "prettier": "^3.6.2",
    "tailwindcss": "^3.4.13",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
