name: Auto Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Run Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies (ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Cache cargo index
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Cache cargo target
      uses: actions/cache@v4
      with:
        path: src-tauri/target
        key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Install frontend dependencies
      working-directory: ./src
      run: npm install

    - name: Run tests
      run: cargo test
      working-directory: ./src-tauri

    - name: Check formatting
      run: cargo fmt --all -- --check
      working-directory: ./src-tauri

    - name: Run clippy
      run: cargo clippy -- -D warnings
      working-directory: ./src-tauri

  build:
    name: Build Application
    needs: test
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            ext: AppImage
          - os: windows-latest
            platform: windows
            ext: msi
          - os: macos-latest
            platform: macos
            ext: dmg

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies (ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Cache cargo index
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Cache cargo target
      uses: actions/cache@v4
      with:
        path: src-tauri/target
        key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('src-tauri/Cargo.lock') }}

    - name: Install frontend dependencies
      working-directory: ./src
      run: npm install

    - name: Build frontend
      run: npm run build
      working-directory: ./src

    - name: Build application
      uses: tauri-apps/tauri-action@v0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tagName: ${{ github.ref_name }}
        releaseName: Libre Browser ${{ github.ref_name }}
        releaseBody: |
          ## 🚀 Libre Browser ${{ github.ref_name }}

          ### 📋 更新内容
          请查看 [CHANGELOG.md](./CHANGELOG.md) 获取详细更新信息

          ### ⬇️ 下载
          选择适合您操作系统的安装包：
          - **Windows**: `.msi` 安装包
          - **macOS**: `.dmg` 磁盘映像
          - **Linux**: `.AppImage` 便携应用

          ### 🔐 安全性
          所有发布包都经过代码签名验证。

          ---
          🤖 *自动发布 - [Tauri Action](https://github.com/tauri-apps/tauri-action)*
        releaseDraft: false
        prerelease: false

  